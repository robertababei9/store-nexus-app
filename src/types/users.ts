export type UserFormType = {
    name: string;
    email: string;
    role: string;
    store: string;
    phoneNumber: string;
    signupDate: string
}

export type SecurityFormType = {
    currentPass: string;
    newPass: string;
    repeatPass: string;
}